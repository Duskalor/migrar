import { Button } from '@mui/material';
import { styled } from '@mui/system';

export const AllBottons = styled(Button)({
  // color: '#fff',
  // backgroundColor: '#00CCFF',
  // '&:hover': { backgroundColor: '#108baa' },
});
